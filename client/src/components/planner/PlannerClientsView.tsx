import React from 'react';
import { SimpleClientDatabase } from '@/components/clients/SimpleClientDatabase';

/**
 * Planner Clients View Component
 *
 * Renders the client database interface within the planner
 * Simple wrapper component for the SimpleClientDatabase
 */
export function PlannerClientsView() {
  return <SimpleClientDatabase />;
}
